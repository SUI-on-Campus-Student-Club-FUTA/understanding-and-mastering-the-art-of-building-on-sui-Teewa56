This is the file that contains how we will deploy the contract

To deploy, we use

```
    sui client publish
```

After running this, we will copy the packageID, and the modules
put the package id in the .toml file